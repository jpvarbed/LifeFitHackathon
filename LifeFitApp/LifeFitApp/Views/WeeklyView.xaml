<Page
    x:Class="LifeFitApp.Views.WeeklyView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LifeFitApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <local:TitleBar Grid.Row="0" Margin="0,-19,0,0" x:Name="Title" Tapped="BackTapped" title="{Binding}"/>
        <ListView Grid.Row="1">
            <TextBlock x:Name="Header" HorizontalAlignment="Right" Margin="0,0,0,0" TextWrapping="Wrap" Text="To Do:" VerticalAlignment="Top" FontFamily="Segoe UI Historic"/>
                <CheckBox x:Name="ActivityCheckBox" Checked="CheckBox_Checked">
                    <StackPanel Orientation="Vertical" Tapped="ActivityTapped">
                        <TextBlock x:Name="ActivityName" HorizontalAlignment="Right" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding}" VerticalAlignment="Top" FontFamily="Segoe UI Historic"/>
                        <Image x:Name="ActivityImage" Margin="0,0,0,0" Source="{Binding}"/>
                    </StackPanel>
                </CheckBox>
        </ListView>
    </Grid>
</Page>
